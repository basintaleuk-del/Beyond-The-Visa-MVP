export default async function handler(req,res){res.setHeader('Access-Control-Allow-Origin',process.env.APP_ORIGIN||'*');res.setHeader('Access-Control-Allow-Headers','Content-Type');if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!process.env.OPENAI_API_KEY)return res.status(503).json({error:'AI not configured'});const{question,history=[],context={}}=req.body||{};if(typeof question!=='string'||!question.trim()||question.length>1000)return res.status(400).json({error:'Invalid question'});const instructions='You are Zibur, a warm, practical relocation companion for international nurses and midwives. Maintain conversational continuity. Use the supplied profile and journey context, never invent missing facts, clearly distinguish general guidance from immigration or legal advice, and direct users to official sources for eligibility, fees and professional registration. Be concise, motivating and ask one useful follow-up question when appropriate.';const input=[...history.slice(-10),{role:'user',content:`Current app context: ${JSON.stringify(context)}\n\nQuestion: ${question}`}];try{const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.OPENAI_MODEL||'gpt-5.4-mini',instructions,input,max_output_tokens:600})});const data=await r.json();if(!r.ok)return res.status(502).json({error:'AI provider error'});return res.status(200).json({answer:data.output_text||'Please try asking that another way.'})}catch{return res.status(502).json({error:'AI connection failed'})}}
