(()=>{'use strict';function fixBooks(){const tab=document.querySelector('#booksAdmin');if(!tab)return;tab.classList.add('booksAdminV71');const heading=tab.querySelector('h2');if(heading)heading.textContent='Books & publishing';const status=tab.querySelector('[role="status"],.message');if(status&&!tab.querySelector('.adminReviewV71'))status.insertAdjacentHTML('afterend','<p class="adminReviewV71"><b>Publishing check:</b> a book appears in the learner library only when its database status is <code>published</code>, its file exists in <code>btv-books</code>, and its pathway permits the current learner.</p>')}
function truth(){document.querySelectorAll('#nclexBankProgressLabel').forEach(x=>x.title='The 2,000-item target includes drafts. Only clinically reviewed active records are learner-visible.');document.querySelectorAll('[data-build-bank="nclex"]').forEach(x=>x.textContent='Build missing NCLEX drafts (target 2,000)')}
function init(){fixBooks();truth()}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>setTimeout(init,100)):setTimeout(init,100);new MutationObserver(()=>{fixBooks();truth()}).observe(document.documentElement,{subtree:true,childList:true});})();
