(function(){
 const db=()=>window.btvSupabase,svg=(name)=>({menu:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M8 6h12M8 12h12M8 18h12"/><circle cx="4" cy="6" r="1"/><circle cx="4" cy="12" r="1"/><circle cx="4" cy="18" r="1"/></svg>',bell:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9M10 21h4"/></svg>',moon:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M20 15.5A8.5 8.5 0 0 1 8.5 4 8.5 8.5 0 1 0 20 15.5Z"/></svg>',clipboard:'<svg viewBox="0 0 24 24" aria-hidden="true"><rect x="5" y="4" width="14" height="17" rx="2"/><path d="M9 4V2h6v2M9 10l1.5 1.5L14 8M9 16h6"/></svg>'}[name]||'');
 function read(k,f){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(f))}catch{return f}}
 async function enhance(){const h=document.getElementById('btvTop73'),root=document.getElementById('dashboardV3');if(!h||!root||root.dataset.v74==='busy')return;root.dataset.v74='busy';
  const mark=h.querySelector('.brandSymbol');if(mark&&!mark.querySelector('img'))mark.innerHTML='<img src="login-logo-v72.png" alt="">';
  let seal=h.querySelector('.memberSeal74');if(!seal){seal=document.createElement('button');seal.className='memberSeal74';seal.type='button';const premium=typeof window.isPremium==='function'&&window.isPremium();seal.innerHTML=`◆<span>${premium?'PREMIUM':'MEMBER'}</span>`;seal.setAttribute('aria-label',premium?'Open Premium membership':'Beyond The Visa member');seal.onclick=()=>window.BTVFeatures?.open('profile');h.append(seal)}
  const tools=h.querySelector('.headerTools73'),wallet=h.querySelector('.walletHead73');if(wallet&&!wallet.querySelector('.walletCoin74'))wallet.firstElementChild.outerHTML='<span class="walletCoin74">◆</span>';
  const notify=h.querySelector('.notificationHead73');if(notify){notify.innerHTML=svg('bell');notify.title='Notifications'}
  let theme=h.querySelector('.themeHead74');if(!theme&&tools){theme=document.createElement('button');theme.className='iconButton73 themeHead74';theme.type='button';theme.innerHTML=svg('moon');theme.setAttribute('aria-label','Toggle dark mode');theme.setAttribute('aria-pressed',String(document.body.classList.contains('dark')));theme.onclick=()=>{document.getElementById('theme')?.click();theme.setAttribute('aria-pressed',String(document.body.classList.contains('dark')))};tools.insertBefore(theme,h.querySelector('.menuWrap73'))}
  const menu=h.querySelector('[data-mobile-open]');if(menu){menu.innerHTML=svg('menu');menu.classList.add('featuresHead74');menu.title='Features menu'}
  let unread=0,cost=50,balance=Number((h.querySelector('[data-wallet-balance]')?.textContent||'0').replace(/[^0-9.-]/g,''))||0;try{const u=(await db()?.auth?.getUser())?.data?.user;if(u){const [n,c,w]=await Promise.all([db().from('btv_notifications').select('id',{count:'exact',head:true}).eq('user_id',u.id).is('read_at',null),db().from('btv_mock_catalog').select('coin_cost').eq('active',true).order('coin_cost').limit(1).maybeSingle(),db().from('btv_wallets').select('balance').eq('user_id',u.id).maybeSingle()]);unread=n.count||0;cost=Number(c.data?.coin_cost||50);balance=Number(w.data?.balance||balance)}}catch(e){console.warn('v74 header data',e)}
  if(notify){notify.querySelector('.notificationBadge74')?.remove();if(unread){const b=document.createElement('span');b.className='notificationBadge74';b.textContent=unread>99?'99+':unread;b.setAttribute('aria-label',`${unread} unread notifications`);notify.append(b)}}
  const next=root.querySelector('.nextAction73');if(next&&!next.querySelector('.nextIcon74'))next.insertAdjacentHTML('afterbegin',`<span class="nextIcon74">${svg('clipboard')}</span>`);
  const walletTile=root.querySelector('.walletTile73');if(walletTile&&!walletTile.querySelector('.walletMini74')){const action=walletTile.querySelector('.action');action?.remove();walletTile.insertAdjacentHTML('beforeend',`<div class="walletMini74"><span><b>${cost} BC</b><small>Standard mock cost</small></span><span><b>${cost?Math.floor(balance/cost):0}</b><small>Mock sessions available</small></span></div><span class="walletButton74">View Wallet</span>`)}
  const journeyTile=root.querySelector('.journeyTile73');if(journeyTile&&!journeyTile.querySelector('.journeyPath74')){const steps=[['Passport','journey','▣'],['IELTS','ielts','▤'],['CBT','cbt','▥'],['NMC','journey','♡'],['Job Search','jobs','▣'],['Visa','journey','◇'],['Arrival','journey','●'],['PIN','journey','✓']];const pct=Number(root.querySelector('.heroStats73 b')?.textContent.replace(/\D/g,''))||0,current=Math.min(steps.length-1,Math.floor(pct/(100/steps.length)));journeyTile.innerHTML=`<h2>Your Journey <span class="action" style="float:right">View Full Journey →</span></h2><div class="journeyPath74">${steps.map((s,i)=>`<button class="stage74 ${i<current?'done':i===current?'current':''}" data-stage="${s[1]}" aria-label="Open ${s[0]}"><span class="stageIcon74">${s[2]}</span><small>${s[0]}</small></button>`).join('')}</div><p>Tip: Continue your current milestone to unlock the next guided resources.</p>`;journeyTile.querySelectorAll('[data-stage]').forEach(b=>b.onclick=e=>{e.stopPropagation();window.BTVFeatures?.open(b.dataset.stage)})}
  const plan=root.querySelector('.planTile73');if(plan&&!plan.querySelector('.planEmpty74')){plan.innerHTML='<h2>Today’s Study Plan <span class="action" style="float:right">View Plan →</span></h2><div class="planEmpty74"><div><p>No study sessions planned yet.</p><button data-create-plan>Create Study Plan</button></div></div>';plan.querySelector('[data-create-plan]').onclick=e=>{e.stopPropagation();window.BTVFeatures?.open('study-plan')}}
  const streak=root.querySelector('.streakTile73');if(streak&&!streak.querySelector('.weekDots74')){const day=new Date().getDay(),value=Number(streak.querySelector('.value')?.textContent)||0;streak.insertAdjacentHTML('beforeend',`<div class="weekDots74">${['S','M','T','W','T','F','S'].map((x,i)=>`<span class="${i===day?'today':''} ${value&&i<day?'done':''}">${x}</span>`).join('')}</div>`)}
  const readiness=root.querySelector('.readinessTile73');if(readiness&&!readiness.querySelector('.readinessNote74'))readiness.insertAdjacentHTML('beforeend','<small class="readinessNote74">Readiness is an estimate based on your platform activity.</small>');
  const photo=read('btv-profile-extra',{}).photo,profile=h.querySelector('.profileButton73');if(photo&&profile&&!profile.querySelector('img'))profile.innerHTML=`<img src="${photo}" alt="Your profile photo">`;
  document.querySelectorAll('#appShell>nav .nav').forEach(n=>{const active=n.classList.contains('active');n.setAttribute('aria-current',active?'page':'false')});root.dataset.v74='done'
 }
 new MutationObserver(()=>setTimeout(enhance,20)).observe(document.documentElement,{childList:true,subtree:true});setTimeout(enhance,1200);window.addEventListener('btv:wallet-changed',()=>setTimeout(enhance,100));
})();
