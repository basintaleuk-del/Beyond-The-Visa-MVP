import { createClient } from 'npm:@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS','Content-Type':'application/json'};
const reply=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:cors});
function client(req:Request){const url=Deno.env.get('SUPABASE_URL'),key=Deno.env.get('SUPABASE_ANON_KEY'),auth=req.headers.get('Authorization');if(!url||!key||!auth)throw Object.assign(Error('Authentication is required.'),{status:401});return createClient(url,key,{global:{headers:{Authorization:auth}},auth:{persistSession:false}})}
const fail=(e:any)=>reply({error:e?.message||'Request failed.'},e?.status||400);Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return reply({error:'Method not allowed'},405);try{const db=client(req),b=await req.json();if(!String(b.reason||'').trim())return reply({error:'Please explain the technical failure.'},400);const {data,error}=await db.rpc('btv_request_mock_refund',{p_session_id:b.session_id,p_reason:String(b.reason).trim()});if(error)throw error;return reply({request_id:data,status:'pending_admin_review'})}catch(e){return fail(e)}});