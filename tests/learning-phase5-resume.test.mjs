import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const read=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');test('paid mocks can be resumed',()=>{for(const file of ['web/cbt.js','web/nclex.js']){const js=read(file);assert.match(js,/saveMockState/);assert.match(js,/restoreMock/)}});test('learning analytics combines CBT and NCLEX activity',()=>{const js=read('web/learning-analytics-v79.js');assert.match(js,/CBT practice accuracy/);assert.match(js,/NCLEX practice accuracy/);assert.match(js,/Recommended next action/)})
