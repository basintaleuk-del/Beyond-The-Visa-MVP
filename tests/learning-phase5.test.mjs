import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const read=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');
test('paid CBT and NCLEX catalogue codes match the browser launchers',()=>{const migration=read('supabase/migrations/202607170007_learning_foundation_phase5.sql'),access=read('web/mock-access-v72.js');for(const code of ['cbt_full','nclex_full']){assert.match(migration,new RegExp(code));assert.match(access,new RegExp(code))}});
test('standalone paid mocks complete their secure wallet sessions',()=>{assert.match(read('web/cbt.js'),/BTVCoins\?\.complete\('cbt_full'/);assert.match(read('web/nclex.js'),/BTVCoins\?\.complete\('nclex_full'/)});
test('daily practice limits are server enforced and configurable',()=>{const sql=read('supabase/migrations/202607170007_learning_foundation_phase5.sql');assert.match(sql,/btv_learning_settings/);assert.match(sql,/before insert on public\.cbt_attempts/);assert.match(sql,/before insert on public\.nclex_attempts/);assert.match(sql,/DAILY_PRACTICE_LIMIT/)});
test('result pages include topic analysis and incorrect answer review',()=>{for(const file of ['web/cbt.js','web/nclex.js']){const js=read(file);assert.match(js,/topicSummary/);assert.match(js,/Review incorrect answers/);assert.match(js,/Revision priorities/)}});
